import './bootstrap';



// // Listen to Laravel Echo channel and event with public channel
// window.Echo.channel('public-messages').listen('MessageSent', (event) => {
//     console.log('Event received', event);
// })

// // Listen to Laravel Echo channel and event with private channel
// const userId = document.querySelector("meta[name='user-id']").getAttribute("content");
// window.Echo.private(`user.${userId}`).listen('.private-notification', (event) => {
//     console.log('Event received', event);
// })


